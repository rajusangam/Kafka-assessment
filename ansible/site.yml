---
- name: Safely update Kafka brokers
  hosts: kafka_brokers
  become: true

  # Never restart multiple Kafka brokers simultaneously.
  serial: 1

  # Stop the rollout if a broker fails.
  max_fail_percentage: 0

  roles:
    - kafka
